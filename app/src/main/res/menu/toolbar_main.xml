<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/toolbar_searchMenu"
        android:icon="@drawable/search_icon"
        android:title="검색"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/toolbar_writeMenu"
        android:icon="@drawable/add_bulletin_icon"
        android:title="작성"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/toolbar_optionMenu"
        android:icon="@drawable/ic_gear_solid"
        android:title="설정"
        android:visible="false"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/toolbar_chatRoomPlus"
        android:icon="@drawable/plus_icon"
        android:title="생성"
        android:visible="false"
        app:showAsAction="ifRoom" />
</menu>